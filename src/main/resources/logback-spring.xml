<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- 日志输出格式常量 -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n" />
    <!-- 日志存放路径（JAR 运行时会自动适配当前目录） -->
    <property name="LOG_HOME" value="./logs" />
    <!-- 日志文件名称 -->
    <property name="LOG_FILE_NAME" value="sqlexecutor" />
    <!-- 日志保留天数 -->
    <property name="MAX_HISTORY" value="30" />

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 按天滚动归档日志（自动压缩昨天的日志） -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 当天日志文件路径 -->
        <file>${LOG_HOME}/${LOG_FILE_NAME}.log</file>

        <!-- 滚动策略：按天切割，每天凌晨归档昨天的日志 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 归档文件命名格式（yyyy-MM-dd 为日期，.gz 自动压缩） -->
            <fileNamePattern>${LOG_HOME}/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <!-- 日志保留天数（超过 30 天的归档日志自动删除） -->
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <!-- 总日志大小限制（可选，防止磁盘占满） -->
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>

        <!-- 日志输出格式 -->
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <!-- 过滤策略：只输出 INFO 及以上级别（可选） -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- 单独输出 ERROR 级别日志（可选，便于排查错误） -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/${LOG_FILE_NAME}-error.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${LOG_FILE_NAME}-error-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <!-- 只输出 ERROR 级别日志 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" /> <!-- 输出到控制台 -->
        <appender-ref ref="FILE" />     <!-- 输出到按天归档的日志文件 -->
        <appender-ref ref="ERROR_FILE" /> <!-- 单独输出错误日志 -->
    </root>

    <!-- 自定义包日志级别（可选，例如降低 Spring 框架的日志级别） -->
    <logger name="org.springframework" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- 你的项目包日志级别（例如 com.dsb 为你的项目根包） -->
    <logger name="com.dsb" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
        <appender-ref ref="ERROR_FILE" />
    </logger>
</configuration>